# Copy to .env and fill in real values. .env is gitignored — NEVER commit real keys.
# This repo is public.

# --- LLM provider API keys (native APIs, one per provider) ---
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-proj-...
XAI_API_KEY=xai-...
GOOGLE_AI_API_KEY=AIza...

# --- Market / odds data sources ---
# the-odds-api.com key with Pinnacle coverage (Pinnacle 3-way closing line, §13)
ODDS_API_KEY=
# Polymarket Gamma API is public — no key needed.
