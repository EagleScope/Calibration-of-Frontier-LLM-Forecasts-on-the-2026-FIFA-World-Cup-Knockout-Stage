# ---- Secrets: NEVER commit ----
.env
.env.*
!.env.example
*.key
credentials
aws_credentials

# ---- Collected data ----
# LIVE registered forecasts ARE committed (§17 rolling timestamps): every per-call
# record is saved AND publicly timestamped. Only pilot/throwaway logs stay ignored.
data/forecasts/pilot*
data/markets/pilot*
!data/**/.gitkeep

# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ipynb_checkpoints/

# ---- OS / editor ----
.DS_Store
*.swp
.idea/
.vscode/

# Demo/scoring artifacts
data/*.png

# throwaway scripts (not part of the study)
scripts/

# Throwaway Elo demo snapshot (real per-match snapshots are committed)
data/packs/elo_snapshots/demo/
